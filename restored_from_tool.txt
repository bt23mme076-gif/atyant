import React, { useState, useEffect, useRef } from 'react';
import { ArrowUpRight, Sparkles } from 'lucide-react';
import { motion, AnimatePresence } from 'framer-motion';
import { API_URL } from '../services/api';

const MOCK_INTENTS = {
  default: {
    context: "VNIT Metallurgy → IIM · 3 paths found",
    analysis: "Many metallurgical engineering students from VNIT successfully pivot to top Indian IIMs. Typically, scoring 99.2+ percentile in CAT and showing diverse extracurriculars bridges the gap. The data suggests that placing focus on specific quantitative skills heavily boosts chances.",
    callout: { label: "// Verified · VNIT → IIM Calcutta · 2024", quote: "I thought my branch would hold me back, but standardizing my CAT prep process allowed me to break through." },
    mentors: [
      { initials: "RK", name: "Rohan K.", role: "MBA Intern · IIM Calcutta", matchScore: 94, tags: ["VNIT", "IIMC"], isTopMatch: true },
      { initials: "PS", name: "Priya S.", role: "Consultant · Mbb", matchScore: 88, tags: ["NIT", "IIMA"], isTopMatch: false }
    ],
    followUps: ["How did they balance college exams and CAT?", "What extracurriculars matter most?"]
  }
};

const detectIntent = (query) => {
  return 'default';
};

const StarCanvas = () => {
  const canvasRef = useRef(null);
  
  useEffect(() => {
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    let animationFrameId;
    
    const resize = () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    };
    window.addEventListener('resize', resize);
    resize();
    
    const stars = Array.from({ length: 90 }, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      radius: 0.3 + Math.random() * 1.2,
      baseOpacity: 0.1 + Math.random() * 0.5,
      phase: Math.random() * Math.PI * 2,
      speed: 0.01 + Math.random() * 0.02
    }));
    
    const render = () => {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      stars.forEach(star => {
        star.phase += star.speed;
        const currentOpacity = star.baseOpacity + Math.sin(star.phase) * 0.2;
        ctx.beginPath();
        ctx.arc(star.x, star.y, star.radius, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(255, 255, 255, ${currentOpacity})`;
        ctx.fill();
      });
      animationFrameId = window.requestAnimationFrame(render);
    };
    render();
    
    return () => {
      window.removeEventListener('resize', resize);
      window.cancelAnimationFrame(animationFrameId);
    };
  }, []);

  return <canvas ref={canvasRef} className="fixed inset-0 z-0 pointer-events-none" />;
};

export default function IntelligenceTerminal() {
  const [messages, setMessages] = useState([]);
  const [isIdle, setIsIdle] = useState(true);
  const [query, setQuery] = useState('');
  const messagesEndRef = useRef(null);

  const scrollToBottom = () => {
    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
  };
  
  useEffect(() => {
    scrollToBottom();
  }, [messages]);

  const handleSubmit = (textQuery) => {
    const q = textQuery || query;
    if (!q.trim()) return;
    
    setIsIdle(false);
    
    const newMsg = { id: Date.now().toString(), role: 'user', content: q };
    setMessages(prev => [...prev, newMsg]);
    setQuery('');
    
    // Simulate AI loading & response
    setTimeout(() => {
      const intent = detectIntent(q);
      const data = MOCK_INTENTS[intent];
      
      setMessages(prev => [...prev, {
        id: (Date.now()+1).toString(),
        role: 'ai',
        content: '',
        response: data
      }]);
    }, 1600);
  };

  const handleKeyDown = (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSubmit();
    }
  };

  return (
    <div className="min-h-screen bg-bg text-t-1 font-sans relative overflow-hidden flex flex-col items-center">
      <StarCanvas />
      
      {/* Dynamic Background dots wrapper */}
      <div className="absolute inset-0 pointer-events-none z-0" 
           style={{
             backgroundImage: 'radial-gradient(circle, rgba(255,255,255,1) 1px, transparent 1px)',
             backgroundSize: '28px 28px',
             opacity: 0.018
           }}>
      </div>

      {/* Header */}
      <header className="w-full fixed top-0 px-6 py-4 flex justify-between items-center z-50 backdro-blur-sm">
        <div className="flex items-center gap-3">
          <div className="font-serif italic text-2xl font-bold tracking-tight text-white flex items-center gap-2">
            atyant
            <div className="w-2 h-2 rounded-full bg-signal animate-pulse-dot" />
          </div>
        </div>
        <div className="flex items-center gap-4 text-sm font-mono font-medium text-t-2">
          <span className="hover:text-t-1 cursor-pointer transition-colors">For Mentors</span>
          <span className="hover:text-t-1 cursor-pointer transition-colors">AnswerCards</span>
          <span className="bg-signal-dim border border-signal-bdr text-signal px-3 py-1 rounded-full text-xs">Seed 2026</span>
        </div>
      </header>

      {/* Main Container */}
      <main className="w-full max-w-[680px] px-4 flex flex-col justify-center min-h-screen z-10 relative pb-32 pt-20">
        
        <AnimatePresence>
          {isIdle && (
            <motion.div 
              initial={{ opacity: 1, height: 'auto' }}
              exit={{ opacity: 0, height: 0, overflow: 'hidden' }}
              className="flex flex-col items-center text-center pt-10"
            >
              <h1 className="font-serif text-5xl md:text-[72px] leading-[1.1] mb-6">
                Find someone who<br/>
                <span className="italic text-t-2">already solved</span><br/>
                your <span className="text-signal">exact</span> problem.
              </h1>
              <p className="text-t-2 text-lg md:text-xl max-w-xl mb-8">
                Type your career question. Atyant matches you with seniors from your background — then shows you their real path.
              </p>
              
              <div className="flex flex-wrap justify-center items-center gap-2 md:gap-4 font-mono text-sm text-t-3 mb-10">
                <span>800+ journeys</span><span className="text-t-2">·</span>
                <span>300+ mentors</span><span className="text-t-2">·</span>
                <span>50+ colleges</span><span className="text-t-2">·</span>
                <span>₹0 paid ads</span>
              </div>
            </motion.div>
          )}
        </AnimatePresence>

        {/* Chat UI */}
        {!isIdle && (
            <div className="flex-1 w-full space-y-6 pt-4 pb-20 overflow-y-auto">
              {messages.map((m) => (
                <div key={m.id} className={`flex flex-col w-full ${m.role === 'user' ? 'items-end' : 'items-start'}`}>
                  {m.role === 'user' ? (
                     <div className="bg-bg-3 border border-border px-4 py-3 rounded-2xl rounded-tr-sm max-w-[85%] text-[15px] leading-relaxed">
                        {m.content}
                     </div>
                  ) : (
                    <div className="w-full animate-in fade-in slide-in-from-bottom-2 duration-300">
                      <div className="flex items-center gap-2 mb-3">
                        <div className="w-5 h-5 bg-signal flex items-center justify-center rounded-sm font-serif text-bg font-bold text-xs">A</div>
                        <div className="font-mono text-xs text-signal uppercase tracking-wider">AtyantEngine</div>
                        {m.response?.context && <div className="ml-auto text-xs font-mono text-t-3">{m.response.context}</div>}
                      </div>
                      
                      {m.response ? (
                        <div className="border-l-2 border-border pl-4 space-y-4 text-t-1 leading-relaxed text-[15px]">
                           <p>{m.response.analysis}</p>
                           <div className="bg-signal-dim border border-signal-bdr p-4 rounded-md font-mono text-sm">
                              <div className="text-signal text-xs mb-2">{m.response.callout.label}</div>
                              <div className="text-t-1">"{m.response.callout.quote}"</div>
                           </div>
                           
                           {/* Mentors */}
                           <div className="pt-4">
                             <div className="font-mono text-xs text-t-3 mb-4">// matched mentors · ranked by background similarity</div>
                             <div className="space-y-3">
                               {m.response.mentors.map((mentor, idx) => (
                                 <motion.div 
                                    initial={{ opacity: 0, y: 10 }}
                                    animate={{ opacity: 1, y: 0 }}
                                    transition={{ delay: 1.8 + idx * 0.1 }}
                                    key={idx}
                                    className={`bg-bg-2 border ${mentor.isTopMatch ? 'border-l-[3px] border-l-signal border-border' : 'border-border'} p-3 rounded-md flex items-center gap-4 hover:border-border-2 transition-colors duration-200`}
                                 >
                                    <div className="w-10 h-10 rounded-md bg-bg-3 font-serif flex items-center justify-center text-t-1 text-lg">{mentor.initials}</div>
                                    <div className="flex-1">
                                      <div className="font-medium">{mentor.name}</div>
                                      <div className="text-xs text-t-2 font-mono mt-0.5">{mentor.role}</div>
                                      <div className="flex gap-2 mt-2">
                                        {mentor.tags.map((tag, tIdx) => (
                                          <span key={tIdx} className={`text-[10px] font-mono px-1.5 py-0.5 rounded ${tIdx < 2 && mentor.isTopMatch ? 'bg-signal-dim text-signal border border-signal-bdr' : 'bg-bg-3 text-t-2'}`}>{tag}</span>
                                        ))}
                                      </div>
                                    </div>
                                    <div className="text-right">
                                      <div className="text-signal font-mono text-lg">{mentor.matchScore}%</div>
                                      <button className="text-xs bg-transparent border border-signal text-signal px-3 py-1 mt-1 rounded hover:bg-signal hover:text-bg transition-colors">Book ↗</button>
                                    </div>
                                 </motion.div>
                               ))}
                             </div>
                           </div>
                           
                           {/* Follow Ups */}
                           <motion.div
                             initial={{ opacity: 0 }}
                             animate={{ opacity: 1 }}
                             transition={{ delay: 2.6 }}
                             className="pt-2"
                           >
                             <div className="font-mono text-xs text-t-3 mb-3">// go deeper</div>
                             <div className="flex flex-wrap gap-2">
                               {m.response.followUps.map((fu, idx) => (
                                 <button key={idx} onClick={() => handleSubmit(fu)} className="bg-bg-2 border border-border text-t-2 text-xs font-mono px-3 py-1.5 rounded-full hover:bg-bg-3 hover:text-t-1 transition-colors">
                                   {fu}
                                 </button>
                               ))}
                             </div>
                           </motion.div>
                        </div>
                      ) : (
                        <div className="flex items-center gap-1 pl-4 h-6">
                           <div className="w-1.5 h-1.5 rounded-full bg-t-3 animate-dot1" />
                           <div className="w-1.5 h-1.5 rounded-full bg-t-3 animate-dot2" />
                           <div className="w-1.5 h-1.5 rounded-full bg-t-3 animate-dot3" />
                        </div>
                      )}
                    </div>
                  )}
                </div>
              ))}
              <div ref={messagesEndRef} />
            </div>
        )}

      </main>

      {/* Input UI */}
      <div className={`w-full fixed ${isIdle ? 'top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px]' : 'bottom-0 left-0'} px-4 z-40 flex flex-col items-center transition-all duration-500 ease-out`} style={{ maxWidth: isIdle ? 680 : '100vw' }}>
          
          <div className={`w-full max-w-[680px] bg-bg-2 border border-border rounded-xl focus-within:border-signal-bdr focus-within:shadow-[0_0_15px_rgba(182,255,60,0.1)] transition-all ${isIdle ? '' : 'mb-6'}`}>
            <div className="px-4 pt-4 pb-2">
              <textarea 
                value={query}
                onChange={e => setQuery(e.target.value)}
                onKeyDown={handleKeyDown}
                placeholder="Describe your situation — college, branch, goal..."
                className="w-full bg-transparent text-t-1 placeholder-t-3 resize-none outline-none text-lg min-h-[50px] max-h-[160px]"
                rows={1}
              />
            </div>
            <div className="flex justify-between items-center px-4 pb-3 border-t border-border mt-1 pt-2">
              <div className="flex gap-2">
                <button className="flex items-center gap-1 text-[11px] font-mono bg-signal-dim border border-signal-bdr text-signal px-2 py-1 rounded">
                   Instant match
                </button>
                <button className="flex items-center gap-1 text-[11px] font-mono text-t-3 px-2 py-1 hover:text-t-2 transition-colors">
                   Deep research
                </button>
              </div>
              <div className="flex items-center gap-3">
                <span className="text-[11px] font-mono text-t-3 hidden sm:inline-block">AtyantEngine v1 ↓</span>
                <button 
                  onClick={() => handleSubmit()}
                  className={`w-8 h-8 rounded-md flex items-center justify-center transition-colors ${query.trim() ? 'bg-signal text-bg' : 'bg-bg-3 text-t-3'}`}
                >
                  <ArrowUpRight size={18} strokeWidth={3} />
                </button>
              </div>
            </div>
          </div>
          
          <AnimatePresence>
            {isIdle && (
              <motion.div 
                 initial={{ opacity: 1, y: 0 }}
                 exit={{ opacity: 0, y: 20 }}
                 className="mt-6 flex flex-wrap justify-center gap-2 max-w-[600px]"
              >
                 {[
                   { icon: '🎯', text: 'IIM from NIT path', q: 'How do I get an IIM internship from VNIT Metallurgy?' },
                   { icon: '🏢', text: 'Tier-3 to product', q: 'Can a Tier-3 college student get into a product company?' },
                   { icon: '🤖', text: 'Mech → AI/ML', q: 'Mechanical engineer switching to AI/ML — what skills?' },
                 ].map((chip, idx) => (
                   <button 
                     key={idx}
                     onClick={() => handleSubmit(chip.q)}
                     className="bg-bg-2 border border-border hover:border-border-2 px-3 py-1.5 rounded-full text-xs font-mono text-t-2 flex items-center gap-1.5 transition-colors"
                   >
                     <span>{chip.icon}</span> {chip.text}
                   </button>
                 ))}
              </motion.div>
            )}
          </AnimatePresence>
      </div>

    </div>
  );
}
import React from 'react';
import IntelligenceTerminal from './IntelligenceTerminal';

const Home = () => {
  return (
    <IntelligenceTerminal />
  );
};

export default Home;