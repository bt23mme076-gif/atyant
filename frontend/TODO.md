# TODO - Make Home Page More Advanced

- [x] Inspect routing/layout/Home entrypoints
- [x] Discover that many `src/components/home/*` and `src/components/common/*` are currently stubbed (return `null`)
- [ ] Implement an advanced Home page using the already-working markup currently embedded in `src/App.jsx`
- [ ] Move that markup into `src/pages/Home.jsx` (and optionally into reusable components later)
- [ ] Implement `src/components/layout/Navbar.jsx` and `src/components/layout/Footer.jsx` (or keep existing `App.jsx` layout temporarily)
- [ ] Implement `src/routes/AppRoutes.jsx` so Home is rendered via routes
- [ ] Run `npm run build` or `npm run dev` to verify compilation

